/**
 * Input behavior settings
 * These can be made user-configurable in the future
 */

/**
 * When true: Enter sends message, Ctrl+Enter adds newline
 * When false: Enter adds newline, Ctrl+Enter sends message
 */
export const ENTER_SENDS_MESSAGE = true;
